# 프로젝트 규칙 (my-app)

## 개요
장비 입고 체크리스트 — 의료장비 협조전 접수부터 입고까지의 절차를 단계별로 체크하는 앱.
자세한 기획 내용은 [PRD.md](PRD.md) 참고.

## 기술 스택 (고정)
- Next.js (App Router) + TypeScript + Tailwind CSS
- `src/` 디렉토리 미사용, import 별칭 `@/*`
- 배포는 Vercel을 사용한다
- 다른 프레임워크로 교체하거나 마이그레이션하는 것을 제안하지 않는다

## 범위
- PRD.md 6번 "이번에 만들 것" 범위 내에서만 개발한다
- PRD.md 6번 "절대 만들지 않을 것" 항목(결재 시스템 연동, 파일 원본 보관, 권한/알림 기능, 통계 리포트 등)은 요청이 있어도 먼저 PRD.md 수정 여부를 확인한 뒤 진행한다
- 새 파일은 my-app 폴더 안에만 만든다

## 핵심 기능 규칙
- **단계별 체크리스트**: 이전 단계가 모두 체크되지 않으면 다음 단계로 진행할 수 없다. 순서를 건너뛰는 로직을 만들지 않는다
- **동시 진행 현황판**: 각 건의 현재 단계는 사용자가 직접 체크한 값만 반영한다. 자동으로 단계를 앞당기거나 완료 처리하지 않는다

## 보안
- 비밀 값은 `.env`에만 두고 코드에 하드코딩하지 않는다
- `.env`, `node_modules`는 항상 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다
- 환자 개인정보는 다루지 않으며, 업체 담당자 연락처 등 최소한의 정보만 기록한다
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다
- Vercel에 배포하기 전에는 `.vercelignore`가 `.gitignore`와 같은 범위로 `.env`·내부 도구 파일(`.bkit`, `.claude` 등)·실제 데이터 파일을 제외하는지 반드시 확인한다 (한 번 배포 결과물에 그대로 올라간 적 있음)

## 파일 삭제 규칙
- 파일을 지워야 할 때는 바로 삭제하지 않는다
- `trash-can` 폴더를 만들어 그 안으로 옮겨두고, 사용자가 직접 확인한 뒤 삭제하도록 한다

## 작업 방식
- 코드를 바꾸면 무엇을 왜 바꿨는지 한 줄로 알려준다
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다

## 작업 절차 (검증 루프)
아래 순서를 통과할 때까지 반복한다.

1) 변경한다
2) `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다
3) 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다 — 레이아웃을 바꿨다면 모바일 폭(375px 안팎)에서도 사이드바 등이 안 깨지는지 함께 확인한다
4) 문제가 있으면 고치고 다시 1)로 돌아간다
5) 모두 통과하면 무엇을 왜 바꿨는지 한 줄로 요약한다

## 코드 스타일
- 모든 설명과 주석은 한국어로 작성한다
- 요청 범위를 벗어난 리팩터링·추상화를 추가하지 않는다
- 주석은 WHY가 비직관적일 때만 작성한다
- 실제로 발생하지 않는 상황에 대한 방어 코드/검증을 추가하지 않는다
