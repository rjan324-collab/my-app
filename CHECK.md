# CHECK — 장비 입고 체크리스트 (2026-09-04)

## 결과: **통과**

PLAN.md의 성공 기준 7개 전부 충족, 보안 점검에서 나온 치명적·High·Medium 항목 전부 수정 완료. 삭제 확인 UX, 체크리스트 날짜 입력, 모바일 반응형 레이아웃까지 추가 반영됐고, 오늘 겪은 문제 2건은 CLAUDE.md 규칙으로 남겨 재발을 막음. 남은 것은 사용자가 위험을 감수하기로 명시적으로 결정한 항목 1개와, 우선순위가 낮아 조치하지 않기로 한 항목뿐이다.

---

## 1. 성공 기준 판정 (PLAN.md 기준)

| # | 기준 | 판정 |
|---|---|---|
| 1 | 6단계가 순서대로만 진행되고, 이전 단계 미완료 시 다음 단계 진행 불가 | 충족 |
| 2 | 여러 건 동시 등록해도 각 건 현재 단계를 헷갈리지 않게 확인 가능 | 충족 |
| 3 | 결재문 기안 서류 5개를 모두 체크해야 그 단계가 완료 처리됨 | 충족 (최초 반대로 동작하던 것을 수정) |
| 4 | 완료 건과 진행 중 건 구분 표시 + 삭제 가능 | 충족 |
| 5 | 데이터가 브라우저(localStorage)에 저장·복원됨 | 충족 |
| 6 | `npm run lint`, `npm run build` 오류 없이 통과 | 충족 |
| 7 | Vercel 배포 + 실제 URL 접근 가능 (더미 데이터만 사용) | 충족 (실제 데이터로 전환하기로 결정 → 비밀번호 게이트로 보완) |

배포 주소: https://my-app-three-nu.vercel.app

---

## 2. Gap 목록 (발견 → 처리 결과)

1. **PRD.md 범위 밖 기능(학년도별 심의 통과 현황)이 이미 구현돼 있었음** → PRD.md 5·6번에 정식 항목(nice-4)으로 반영, "통계·리포트 자동 생성 금지" 조항에 예외 명시
2. **결재문 기안 체크박스가 서류 확인 없이 자동으로 5개를 다 채움** (성공 기준 3번 위반) → 서류 5개를 개별로 체크해야 단계가 자동 완료되도록 로직 반전, 서류 체크박스에도 이전 단계 완료 조건 추가
3. **`public/seed-2026.json`이 실제 병원 구매 데이터였음** (더미 데이터 아님) → 사용자 확인 후 실제 데이터 사용을 전제로 비밀번호 게이트 도입 결정, PRD.md 7번 갱신

---

## 3. 보안 점검 결과 (bkit security-architect, 2026-09-04)

| 심각도 | 항목 | 처리 |
|---|---|---|
| Critical | `SITE_PASSWORD` 미설정 시 게이트가 fail-open으로 뚫림 | 수정 완료 (`proxy.ts`) |
| Critical | 실데이터(`seed-2026.json`)가 비밀번호 없이 공개돼 있었음 | 수정 완료 — `data/`로 이동, 게이트 걸린 `/api/seed`로만 서빙 |
| High | 로그인 리다이렉트 `from` 파라미터로 인한 오픈 리다이렉트 | 수정 완료 — 상대경로만 허용 |
| High | 로그인 무차별 대입 방지 없음 | 완화 완료 — 실패 시 1초 지연 추가 (비밀번호 자체는 사용자 결정으로 유지) |
| Medium | 로그인 쿠키에 비밀번호 평문 저장 (DevTools 노출) | 수정 완료 — SHA-256 해시로 대체 |
| Medium | 심의자료 JSON 가져오기 구조 검증 없음 → 잘못된 파일 시 화면 영구 손상 가능 | 수정 완료 — `isValidEquipmentArray` 검증 추가 |
| Medium | `.gitignore`에 실제 업무 파일(xlsx 등) 누락 | 수정 완료 |
| Low | 보안 헤더 없음 (클릭재킹 등) | 수정 완료 — `X-Frame-Options`, `X-Content-Type-Options` 추가 |
| Low | 쿠키 비교가 상수시간 비교가 아님 | 조치 불필요로 판단 (위협 모델상 실익 없음) |
| Low | `/loginabc` 같은 경로가 matcher에서 접두사 매칭됨 | 조치 불필요 (실질 피해 없음) |

프롬프트 인젝션(챗봇 없음), 개인정보(연락처 필드뿐이며 현재 값 전부 빈 문자열, 외부 전송 경로 없음)는 별도 위험 없음으로 확인.

---

## 4. UX 개선 (2026-09-04)

현황판 "삭제" 버튼에 확인 절차 추가. 서버 백업 없이 브라우저에만 저장되는 실제 데이터라 실수로 삭제하면 되돌릴 방법이 없다는 점을 가장 큰 사용자 경험 위험으로 판단해 선택함. `window.confirm()`으로 건 이름을 보여주며 확인받고, 취소하면 아무 일도 일어나지 않음 (`app/home-client.tsx`). lint/build 통과, 브라우저 콘솔에서 확인 메시지가 정확히 뜨는 것 확인 후 재배포 완료.

---

## 5. 기능 추가 — 체크리스트 날짜 입력 (2026-09-04)

사용자 요청으로 협조전 전송·접수·계약일자 확정 단계에 날짜 입력란 신설, "납품일자"를 "납품 예정일자"로 명칭 정정, 입고·검수 단계에 입고일자 직접 입력 기능 추가. 이에 맞춰 입고일자가 체크 해제 시 자동으로 지워지던 로직도 제거함(직접 입력한 사실은 체크 여부와 무관하게 남아야 하므로). lint/build 통과, 입력→저장→새로고침 유지 확인 후 재배포 완료.

---

## 6. 반응형(모바일) 레이아웃 추가 (2026-09-04)

"핸드폰에서도 쓸 것"이라는 요청에 확인해보니 폰 화면(375px)에서 고정폭 사이드바가 화면 절반을 차지해 사실상 못 쓰는 수준이었음. 네이티브 앱이 아니라 지금 웹앱을 폰에서도 잘 보이게 하는 방향으로 확정. `app/app-shell.tsx` 신설 — 데스크톱은 기존 사이드바 그대로, 폰 화면은 햄버거 버튼으로 여닫는 오버레이 메뉴로 전환. 375px·1280px 두 크기 모두 브라우저에서 확인 후 재배포 완료.

---

## 7. 재발 방지 규칙 (CLAUDE.md 반영, 2026-09-04)

오늘 실제로 겪은 문제 2건을 CLAUDE.md 규칙으로 추가함:
1. **Vercel 배포 전 `.vercelignore` 확인 의무화** — 최초 배포 때 이 파일이 없어서 `.env`·`.bkit`·`.claude`·실제 데이터 파일이 배포 결과물에 그대로 올라갔던 사고 재발 방지 (보안 섹션에 추가)
2. **레이아웃 변경 시 모바일 폭 확인 의무화** — 사이드바가 폰 화면을 다 차지했던 문제 재발 방지 (작업 절차·검증 루프 3단계에 추가)

---

## 8. 남은 항목 (우선순위 순)

1. **비밀번호가 짧아 무차별 대입에 여전히 취약함** — 사용자가 기존 비밀번호를 유지하기로 결정 (1초 지연으로 완화만 적용됨). 더 강화하려면 비밀번호를 길고 무작위한 값으로 교체
2. **[Low, 미수정] `lib/use-equipments.ts`의 seed fetch가 `res.ok`를 확인하지 않음** — 로그인 페이지 HTML 등이 잘못 반환되면 조용히 실패해 원인 파악이 어려움. 보안 문제는 아니고 사소한 견고성 개선 사항
