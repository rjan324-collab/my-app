# DESIGN — 장비 입고 체크리스트

## 전체 앱 뼈대

- 앱 이름: "의료장비 업무 도우미"
- 왼쪽에 고정 사이드바를 두고, 로고 + 앱 이름 + 업무 섹션 목록(nav)을 보여준다
- 지금은 "장비 입고 체크리스트" 섹션 하나뿐이며 항상 선택된 상태로 표시된다
- 앞으로 다른 업무가 추가되면 사이드바 nav 목록에 항목만 추가하면 된다 (레이아웃 구조는 그대로 재사용)

## 화면 구성

**메인 화면 (현황판)**
- 상단: "새 장비 건 등록" 버튼, 진행 중 / 완료 필터
- 그 아래: 등록된 장비 건들을 카드(또는 행)로 나열
  - 카드 안에는 건 이름, 현재 단계(예: "3/6 결재문 기안"), 진행률, 완료 여부(완료 배지) 표시
  - 카드를 클릭하면 그 건의 체크리스트 화면으로 이동
  - 카드에는 삭제 버튼이 있어 잘못 등록한 건을 지울 수 있음

**등록 폼 (모달)**
- "새 장비 건 등록" 버튼을 누르면 현황판 화면 위에 겹쳐서 뜨는 모달 창 (별도 페이지로 이동하지 않음)
- 입력 항목: 건 이름, 업체 담당자 연락처
- 저장하면 모달이 닫히고, 6단계가 모두 미체크 상태인 새 건이 현황판 목록에 추가됨
- 닫기(X) 또는 바깥 영역 클릭 시 입력 내용 없이 모달만 닫힘

**체크리스트 화면 (건 상세)**
- 6단계가 세로로 나열되고, 각 단계에 체크박스가 있음
  1. 협조전 전송
  2. 접수
  3. 결재문 기안 (하위에 필요 서류 5개 체크 항목 — 가격대비표, 견적서, 신청서, 타병원실적, 계약서. **5개를 모두 체크해야 이 단계가 완료 처리됨**)
  4. 계약일자 확정
  5. 계약서·납품일 확인 (납품일자 입력란 포함)
  6. 입고·검수
- 완료된 단계는 체크 표시, 현재 진행 중인 단계는 강조 표시
- 이전 단계가 체크되지 않으면 다음 단계 체크박스는 눌러지지 않음(비활성)
- 이미 체크된 단계를 해제하면, 그 뒤에 체크돼 있던 단계들도 함께 자동으로 해제됨
- 완료 여부는 별도로 저장하지 않고, 6단계가 모두 체크됐는지를 그때그때 계산해서 "완료" 상태로 보여준다

## 데이터 흐름

1. **입력**: 사용자가 새 건을 등록하거나(건 이름, 연락처), 체크리스트 화면에서 단계를 체크·해제
2. **처리**: 이전 단계가 모두 체크됐는지(3단계는 서류 5개까지) 확인한 뒤에만 체크를 허용하고, 해제 시 이후 단계도 함께 해제 처리한 뒤 브라우저(localStorage)에 저장
3. **출력**: 저장된 내용이 체크리스트 화면과 메인 현황판(진행률·완료 배지·필터)에 즉시 반영됨

## 기술 선택

- **Next.js (기본)**: 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리하고, Part 5에서 Vercel로 그대로 배포한다.
- **브라우저 저장 (localStorage)**: 장비 건 데이터를 별도 서버나 외부 클라우드 없이 이 브라우저 안에만 저장한다. 로그인이나 추가 설정 없이 바로 쓸 수 있지만, 다른 컴퓨터·다른 담당자와는 데이터가 공유되지 않는다. (PRD 6·7번에 따라 외부 클라우드 저장은 이번 사이클 범위에서 제외했다.)
