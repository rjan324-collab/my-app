# PLAN — 장비 입고 체크리스트

## 이번 사이클 목표
PRD.md에 정의된 must 기능 2개(단계별 체크리스트, 동시 진행 현황판)와 nice 기능(필요 서류 체크)을 모두 구현해, 장비 입고 절차를 빠짐없이 관리할 수 있는 앱을 완성한다.

## 성공 기준
- 6단계 체크리스트가 순서대로만 진행되고, 이전 단계 미완료 시 다음 단계로 못 넘어간다
- 여러 장비 건을 동시에 등록해도 각 건의 현재 단계를 헷갈리지 않고 확인할 수 있다
- 결재문 기안 단계의 필요 서류 5개를 모두 체크해야 그 단계가 완료 처리된다
- 완료된 건과 진행 중인 건이 구분되어 보이고, 불필요한 건은 삭제할 수 있다
- 데이터는 브라우저(localStorage)에 저장·복원된다
- `npm run lint`, `npm run build`가 오류 없이 통과한다
- Vercel에 배포되어 실제 URL로 접근 가능하다 (실습용 더미 데이터만 사용)

*(PRD 3번의 정량 목표인 재작업 80% 감소·완료율 95% 등은 이번 사이클에서 측정하지 않는 장기 목표다.)*

## 작업 순서

1. 장비 건 데이터 모델 정의 — 건 이름, 업체 담당자 연락처, 납품일자, 6단계 각각의 체크 여부, 결재문 기안 서류 5개 체크 여부, 완료 여부
2. 장비 건 등록·체크리스트 화면 뼈대 만들기
3. 장비 건 등록 폼 구현 (건 이름, 업체 담당자 연락처 등 입력)
4. 6단계 체크리스트 항목 표시 및 체크 기능 구현
5. 이전 단계 미완료 시 다음 단계로 못 넘어가게 막는 순서 제어 로직 구현 (단계 해제 시 이후 단계 연쇄 해제 포함)
6. 결재문 기안 단계 필요 서류 5개 체크 항목 구현 (5개 모두 체크돼야 3단계 완료 처리)
7. 건별 진행 상태를 브라우저(localStorage)에 저장·불러오는 기능 구현
8. 완료된 건 표시 및 삭제 기능 구현
9. 여러 건을 목록으로 보여주는 현황판 화면 구현 (진행 중 / 완료 필터 포함)
10. 현황판에 각 건의 현재 단계 표시 기능 구현
11. `npm run lint`, `npm run build`로 오류 확인 및 수정
12. Vercel 배포 설정 확인 및 배포 (실습용 더미 데이터만 사용)
13. (추가) 학년도별 심의 통과 현황 화면 구현 — 정책/구입/공동운영/임대 구분별 집계, 구입 장비 네고율 계산, 심의자료(JSON) 가져오기 (PRD 5번 nice-4)

## 진행 현황 (2026-09-04 확인)
- 1~10, 13번: 구현 완료. 브라우저로 현황판 → 등록 → 체크리스트(순서 제어·연쇄 해제) → 심의 통과 화면까지 직접 켜서 정상 동작 확인함
- 11번: `npm run lint`, `npm run build` 통과 확인
- 12번: 배포 완료 → https://my-app-three-nu.vercel.app
  - 최초 배포 시 `.env`(토큰), `.bkit`/`.claude` 내부 로그, 실제 데이터 파일(`★ 심의자료.xlsx`)이 결과물에 함께 올라간 것을 발견해 즉시 프로젝트를 삭제하고, `.vercelignore`를 추가해 다시 배포함. 배포 파일 목록을 재확인해 위 파일들이 빠진 것을 확인함
  - 다음에 다시 배포할 때도 `.vercelignore`가 함께 있는지 반드시 확인할 것
  - `public/seed-2026.json`은 실제 병원 구매 데이터로 확인됨. Vercel Hobby 플랜은 유료 비밀번호 보호를 지원하지 않아, 대신 앱에 자체 비밀번호 게이트를 추가함(`proxy.ts`, `app/login`, `app/api/login`). 배포 URL 접속 시 비밀번호 없이는 `/login`으로 막히는 것까지 확인 완료 → 7번 기준도 충족으로 판정
  - Vercel 토큰이 배포 도중 만료돼 재발급받는 일이 있었음. 다음에 토큰이 안 될 때는 먼저 https://vercel.com/account/tokens 에서 만료 여부부터 확인할 것

## 보안 점검 결과 (2026-09-04, bkit security-architect)
배포 전 점검에서 비밀번호 게이트가 실제로는 뚫려 있는 심각한 문제들을 발견해 즉시 수정함:
- **[치명적] 환경변수 누락 시 게이트가 통째로 열림(fail-open)** — `SITE_PASSWORD` 미설정 시 `undefined === undefined`로 통과되던 버그. `proxy.ts`에서 값이 있을 때만 통과하도록 수정
- **[치명적] `seed-2026.json`(실제 구매데이터)이 비밀번호 없이 그대로 공개돼 있었음** — matcher가 점(.)이 든 모든 경로를 게이트에서 제외해서 발생. `public/`에서 빼서 `data/seed-2026.json` + 게이트가 걸리는 `/api/seed` 라우트로 서빙하도록 변경. 배포 후 옛 경로가 막히고 실데이터는 로그인해야만 받아지는 것 확인함
- **[High] 오픈 리다이렉트** — 로그인 폼의 `from` 값을 검증 없이 리다이렉트에 써서 `?from=https://evil.com` 같은 피싱 링크가 가능했음. 상대경로("/"로 시작)만 허용하도록 수정
- **[High] 로그인 무차별 대입 방지 없음** — 비밀번호를 더 긴 값으로 바꾸는 방안을 제안했으나, 사용자가 기존 비밀번호를 그대로 쓰기로 결정함. 무차별 대입 위험은 감수하는 것으로 확인 (비밀번호 값 자체는 `.env`의 `SITE_PASSWORD`에만 있음, 문서에는 적지 않음)
- **[Medium] 쿠키 비밀번호 평문 저장 → 해시로 변경 완료**: `lib/site-auth.ts`에 SHA-256 해시 함수 추가, `proxy.ts`·`app/api/login/route.ts` 양쪽에서 원문 대신 해시로 비교·저장하도록 수정. 로컬·실배포 모두 쿠키 값이 해시(평문 아님)로 저장되는 것 확인함
- **[Medium] JSON 가져오기 구조 검증 없음 → 검증 추가 완료**: `lib/equipment.ts`에 `isValidEquipmentArray` 추가(6단계·서류 5개 키 타입까지 확인, 하나라도 어긋나면 전체 거부). `review-client.tsx`에 적용. 잘못된 형식 파일은 거부 메시지, 정상 파일은 가져와지고 덮어쓴 건수까지 안내하는 것을 브라우저에서 직접 테스트함
- `.gitignore`에 `*.xlsx`/`trash-can`/`.bkit` 추가함(완료)
- **[L-1] 보안 헤더 추가 완료**: `next.config.ts`에 `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff` 추가. 로컬·실배포 응답 헤더에서 확인함
- **[H-1] 로그인 실패 시 1초 고정 지연 추가 완료**: 비밀번호는 그대로 두되(`app/api/login/route.ts`), 틀린 비밀번호 요청에 1초 지연을 넣어 무차별 대입 속도를 늦춤. 정상 로그인은 지연 없음, 틀린 요청만 ~1.1초 걸리는 것 확인함
- 이번 보안 점검 항목은 모두 처리 완료 (남은 리스크는 짧은 비밀번호 자체 — 사용자가 감수하기로 함)

## UX 개선 (2026-09-04)
현황판 "삭제" 버튼에 확인 절차 추가. 서버 백업 없이 브라우저에만 저장되는 실제 데이터라, 실수로 삭제하면 되돌릴 방법이 없다는 점이 가장 큰 사용자 경험 위험으로 판단해 선택함. `window.confirm()`으로 건 이름을 보여주며 확인받고, 취소하면 아무 일도 일어나지 않도록 구현 ([app/home-client.tsx](app/home-client.tsx)). lint/build 통과, 브라우저 콘솔에서 확인 메시지가 정확히 뜨는 것 확인 후 재배포함

## 체크리스트 날짜 입력 기능 추가 (2026-09-04)
사용자 요청으로 상세 화면에 날짜 입력란을 추가함:
- 협조전 전송·접수·계약일자 확정 단계에 각각 날짜 입력란 추가 (`cooperationRequestDate`, `receiptDate`, `contractConfirmedDate` 필드 신설)
- "계약서·납품일 확인" 단계의 "납품일자" 라벨을 "납품 예정일자"로 변경 (필드명은 `deliveryDate` 그대로)
- "입고·검수" 단계에 입고일자(`receivedAt`) 입력란 추가 — 기존엔 체크 시 자동 기록만 되고 화면에 입력란이 없었음
- 위 변경에 따라 `withReceivedAt`도 수정: 입고일자를 이제 직접 입력할 수 있으므로, 입고·검수 체크를 해제해도 더 이상 입고일자를 지우지 않도록 바꿈 (실제 입고일은 체크 여부와 별개의 사실이므로)
- 옛 데이터(새 필드 없음)에도 안전하도록 화면에서 빈 문자열로 대체 표시
- lint/build 통과, 브라우저에서 입력→localStorage 저장→새로고침 후 유지, 체크 해제해도 입고일자 안 지워지는 것까지 확인 후 재배포함

## 반응형(모바일) 레이아웃 추가 (2026-09-04)
"핸드폰 어플로도 만들 것"이라는 요청에 확인해보니, 지금 웹앱은 폰 화면(375px)에서 사이드바가 고정폭으로 화면 절반을 차지해 못 쓰는 수준이었음. 네이티브 앱(React Native 등, 별도 프로젝트·기술스택 필요)이 아니라 지금 웹앱을 폰에서도 잘 보이게 하는 방향으로 사용자가 확정함.
- `app/app-shell.tsx` 신설: 데스크톱(`sm:` 이상)에서는 기존처럼 사이드바가 항상 보이고, 폰 화면에서는 상단 햄버거 버튼으로 여닫는 오버레이 메뉴로 바뀜
- `app/layout.tsx`를 AppShell을 쓰도록 단순화, `app/nav-links.tsx`에 `onNavigate` 콜백 추가(모바일에서 메뉴 클릭 시 자동으로 닫힘)
- lint/build 통과. 브라우저에서 모바일(375px)·데스크톱(1280px) 두 크기 모두 확인 — 모바일에서 햄버거로 드로어가 열리고 닫히는 것, 데스크톱에서 기존 사이드바가 그대로 보이는 것 확인 후 재배포함
- 점검 리포트 전문은 대화 기록 참고

## 성공 기준 점검 결과 (2026-09-04)
- 3번째 기준("결재문 기안 서류 5개를 모두 체크해야 완료 처리") 미충족 발견 → 수정 완료
  - 문제: 서류를 확인 안 하고 "결재문 기안" 체크박스만 눌러도 서류 5개가 자동으로 다 체크됐음
  - 수정: 결재문 기안 체크박스는 서류 5개 체크 결과를 보여주기만 하는 표시로 바꾸고(직접 클릭 불가), 서류 5개를 다 체크해야 그 순간 자동으로 완료 처리되도록 변경. 서류 체크박스도 "접수" 단계 전에는 못 누르게 막음 ([lib/equipment.ts](lib/equipment.ts), [app/equipment/[id]/detail-client.tsx](app/equipment/[id]/detail-client.tsx))
  - lint/build 통과 확인, 브라우저로 체크→자동완료→해제 시 연쇄 해제까지 직접 확인함
